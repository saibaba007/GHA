name: event Push
on: 
  push:
    branches:
      - main
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run: |
         echo "this is push event for all branches"
